version: '3'
services:
  billboard-service-api:
    image: registry.gitlab.com/ucconnect.devs/community-billboard/service-api:staging
    container_name: billboard-service-api
    env_file:
      - ".env-service-api.staging"
    networks:
      - billboard-network
    command: node dist/apps/auth
    ports:
      - 3301:3000
    restart: always

  # nginx-auth:
  #   image: nginx:stable-alpine
  #   container_name: auth
  #   volumes:
  #     - ./nginx-auth.conf:/etc/nginx/conf.d/default.conf
  #   depends_on:
  #     - service-auth
  #   networks:
  #     - billboard-network

  # service-payment:
  #   image: registry.gitlab.com/ucconnect.devs/community-billboard/service-api:staging
  #   env_file:
  #     - ".env-auth.staging"
  #   networks:
  #     - billboard-network
  #   command: node dist/apps/payment
  #   restart: always

  # nginx-payment:
  #   image: nginx:stable-alpine
  #   container_name: payment
  #   volumes:
  #     - ./nginx-payment.conf:/etc/nginx/conf.d/default.conf
  #   depends_on:
  #     - service-payment
  #   networks:
  #     - billboard-network

  # service-playback:
  #   image: registry.gitlab.com/ucconnect.devs/community-billboard/service-api:staging
  #   env_file:
  #     - ".env-auth.staging"
  #   networks:
  #     - billboard-network
  #   command: node dist/apps/playback
  #   restart: always

  # nginx-playback:
  #   image: nginx:stable-alpine
  #   volumes:
  #     - ./nginx-playback.conf:/etc/nginx/conf.d/default.conf
  #   container_name: playback
  #   depends_on:
  #     - service-playback
  #   networks:
  #     - billboard-network

  billboard-service-search:
    image: registry.gitlab.com/ucconnect.devs/community-billboard/service-api:staging
    container_name: billboard-service-search
    env_file:
      - ".env-service-api.staging"
    networks:
      - billboard-network
    command: node dist/apps/search
    restart: always
    ports:
      - 3302:3000

  # nginx-search:
  #   image: nginx:stable-alpine
  #   volumes:
  #     - ./nginx-search.conf:/etc/nginx/conf.d/default.conf
  #   container_name: search
  #   depends_on:
  #     - service-search
  #   networks:
  #     - billboard-network

  billboard-service-consumer:
    image: registry.gitlab.com/ucconnect.devs/community-billboard/service-api:staging
    container_name: billboard-service-consumer
    env_file:
      - ".env-service-api.staging"
    networks:
      - billboard-network
    command: node dist/apps/mq-consumer
    restart: always

  billboard-service-media:
    image: registry.gitlab.com/ucconnect.devs/community-billboard/service-api:staging
    container_name: billboard-service-media
    env_file:
      - ".env-service-api.staging"
    networks:
      - billboard-network
    command: node dist/apps/media
    restart: always
    ports:
      - 3304:3000

  billboard-service-payment:
    image: registry.gitlab.com/ucconnect.devs/community-billboard/service-api:staging
    container_name: billboard-service-payment
    env_file:
      - ".env-service-api.staging"
    networks:
      - billboard-network
    command: node dist/apps/payment
    restart: always
    ports:
      - 3305:3002

  # service-scheduler:
  #   image: registry.gitlab.com/ucconnect.devs/community-billboard/service-api:staging
  #   env_file:
  #     - ".env-auth.staging"
  #   networks:
  #     - billboard-network
  #   command: node dist/apps/scheduler
  #   restart: always

  billboard-service-broadcast:
    image: registry.gitlab.com/ucconnect.devs/community-billboard/service-api:staging
    container_name: billboard-service-broadcast
    env_file:
      - ".env-service-api.staging"
    networks:
      - billboard-network
    command: node dist/apps/broadcast
    restart: always
    ports:
      - 3303:3000

networks:
  billboard-network:
    external: true